export const metadata = {
  title: "How to get complete path to a file in terminal",
  description: "Use readlink -f to get the complete absolute path to a file in the terminal.",
  date: "2022-11-11",
  tags: ["Shell"],
  published: true,
};

```shell
readlink -f <file_name>
```
