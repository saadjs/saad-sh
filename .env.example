# Neon / Postgres (used by drizzle + neon demos)
# These are automatically created by Neon Launchpad (https://neon.new).
DATABASE_URL=
DATABASE_URL_POOLER=

# Better Auth (used by the better-auth integration + /demo/better-auth)
BETTER_AUTH_URL=http://localhost:3000
# Generate with: npx -y @better-auth/cli secret
BETTER_AUTH_SECRET=

# Sentry (error monitoring)
VITE_SENTRY_DSN=
VITE_SENTRY_ORG=
VITE_SENTRY_PROJECT=
SENTRY_AUTH_TOKEN=
