# No environment variables are required for the blog itself.
# Add any future secrets here; copy to .env.local for local dev and
# set them on Cloudflare via `wrangler secret put <NAME>` for production.
